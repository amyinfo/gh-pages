apiVersion: v1
kind: Service
metadata:
  name: {{ include "pat.fullname" . }}
  labels:
    {{- include "pat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pat.selectorLabels" . | nindent 4 }}
