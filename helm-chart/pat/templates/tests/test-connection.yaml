apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pat.fullname" . }}-test-connection"
  labels:
    {{- include "pat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
